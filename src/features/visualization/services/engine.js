export const styles={normal:"Normal",travel:"Travel",transport:"Transport",vehicle:"Vehicle"};
export function drawFrame(ctx,w,h,points,progress,style="normal"){ctx.fillStyle="#11110f";ctx.fillRect(0,0,w,h);if(!points.length)return;const la=points.map(p=>p.lat),lo=points.map(p=>p.lng),pad=Math.min(w,h)*.12,minLa=Math.min(...la),minLo=Math.min(...lo),sl=Math.max(.01,Math.max(...la)-minLa),so=Math.max(.01,Math.max(...lo)-minLo),m=p=>({x:pad+(p.lng-minLo)/so*(w-2*pad),y:h-pad-(p.lat-minLa)/sl*(h-2*pad)}),end=Math.max(1,Math.floor(progress*(points.length-1))),q=points.slice(0,end+1);ctx.lineCap="round";ctx.strokeStyle=style==="transport"?"#d6f45e":"#eee9dc";ctx.lineWidth=Math.max(3,w*.004);ctx.beginPath();q.forEach((p,i)=>{const v=m(p);i?ctx.lineTo(v.x,v.y):ctx.moveTo(v.x,v.y)});ctx.stroke();if(style==="travel"){ctx.strokeStyle="#fff3";ctx.lineWidth=1;ctx.strokeRect(pad,pad,w-pad*2,h-pad*2)}const c=m(q.at(-1));ctx.fillStyle=style==="vehicle"?"#fc6f4f":"#f7f3ea";ctx.beginPath();ctx.arc(c.x,c.y,style==="vehicle"?Math.max(9,w*.015):Math.max(6,w*.009),0,Math.PI*2);ctx.fill();}
